# BACKLOG - ziftsieve
DONE 2026-07-14 src/zstd/huffman.rs:68 | bug | high | Canonical Huffman tree validation requires the Kraft-McMillan sum to be in 0.999..=1.001
src/snappy.rs:68 | ziftsieve | silent-fallback | medium | REPOPULATE 2026-07-16: uncompressed snappy chunk of length less than or equal to 4 bytes is silently ignored instead of returning a malformed error | return Err(ZiftError::InvalidData) for uncompressed chunks with length <= 4, proven by a test asserting that a 4-byte uncompressed chunk returns ZiftError | status=done | FIXED (Law-10): the 0x01 uncompressed-chunk arm in `handle_snappy_chunk` now returns `ZiftError::InvalidData { offset, reason }` when `chunk_data.len() <= 4` (only the 4-byte CRC-32C, no payload) instead of silently `extend_from_slice(&chunk_data[4..])` on an empty slice - a malformed chunk was being accepted as a zero-literal contribution with no signal, dropping data invisibly. Proving tests snappy::tests::uncompressed_chunk_of_only_crc_is_rejected_not_silently_dropped (a length-4 CRC-only chunk => Err(InvalidData)) + uncompressed_chunk_with_one_data_byte_is_accepted (length-5, guards against over-rejection: the single data byte after the CRC is extracted). Gate: cargo test -p ziftsieve --lib --features snappy,gzip 39 passed, EXIT=0.
src/gzip/deflate.rs:349 | ziftsieve | performance | medium | REPOPULATE 2026-07-16: literals vector of the compressed block is initialized with capacity zero and repeatedly grown/reallocated by one byte at a time in the hot Huffman decoding loop | preallocate or reserve capacity in the block literals vector based on the compressed block size, proven by a test asserting that the literals vector does not repeatedly reallocate during Huffman decoding | status=done | FIXED (Law-7): `parse_huffman_block_with_limit` now reserves the literals buffer once before the decode loop - `if block.literals.is_empty() { let estimate = reader.remaining_bytes().min(MAX_BLOCK_LITERALS); block.literals.reserve(estimate); }` - so a block no longer regrows one byte at a time from capacity 0 (each literal consumes >= 1 input byte, so remaining_bytes is a correct upper bound; capped at MAX_BLOCK_LITERALS = 16 MiB to bound the reservation on hostile input). Proving test gzip::tests::huffman_literals_buffer_is_reserved_upfront_not_regrown_from_zero: a 100k single-byte run compresses to a few literals but a ~114-byte block; asserts capacity >= compressed_len/2 (compressed-block scale), which from-zero doubling (capped near MIN_NON_ZERO_CAP=8 for a Vec<u8>) cannot reach, with a guard that literals*8 < compressed_len. Gate: 39 passed, EXIT=0.
src/snappy.rs:230 | ziftsieve | performance | low | REPOPULATE 2026-07-16: std::mem::take(literals) clears the backing capacity of the streaming literals vector to zero upon block flushing, causing subsequent chunks to trigger multiple reallocations | swap or reset the vector without discarding its capacity, proven by a test asserting that the literals vector retains its capacity after block flushing | status=done | FIXED (Law-7): `flush_block` now captures `let flushed_capacity = literals.capacity();` before `std::mem::take(literals)` (which moves the whole allocation into the block and leaves the streaming buffer at capacity 0) and immediately `literals.reserve(flushed_capacity)`, so the reused buffer starts the next chunk sized to the previous block instead of regrowing from scratch (no copy: the bytes still move into the block). Proving test snappy::tests::flush_block_retains_literals_capacity_for_reuse: a literals vec with capacity 4096 holding 100 bytes, after flush the block owns the 100 literals, the streaming buffer is empty AND capacity() >= 4096 (the old mem::take left it at 0). Gate: 39 passed, EXIT=0.